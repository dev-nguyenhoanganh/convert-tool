{
  "name": "convert-tool",
  "version": "0.1.0",
  "main": "main.js",
  "private": true,
  "dependencies": {
    "date-fns": "^2.30.0",
    "iconv-lite": "^0.6.3",
    "typescript": "^4.9.5",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager \"C:\\Web Developer\\Side Project\\convert-tool\" convert-tool --platform=win32 --arch=x64 --overwrite",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "^26.3.0",
    "electron-packager": "^17.1.2",
    "electron-reloader": "^1.2.3"
  }
}
